services:
  aiagent-hub:
    build: .
    container_name: aiagent-hub
    restart: unless-stopped
    ports:
      - "3000:80"
    volumes:
      - aiagent-hub-data:/app/data
    environment:
      - NODE_ENV=production

volumes:
  aiagent-hub-data:
